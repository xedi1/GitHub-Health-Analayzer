// Source code is decompiled from a .class file using FernFlower decompiler (from Intellij IDEA).
package com.healthanalyzer.model;

import java.util.List;

public record DependencyHealth(List<String> manifestFiles, int totalDependencies, int directDependencies, int outdatedCount, int deprecatedCount, boolean hasVulnerabilities) {
   public boolean isHealthy() {
      if (this.hasVulnerabilities) {
         return false;
      } else if (this.totalDependencies == 0) {
         return true;
      } else {
         return (double)this.outdatedCount < (double)this.totalDependencies * (double)0.5F;
      }
   }

   public double outdatedRatio() {
      return this.totalDependencies == 0 ? (double)0.0F : (double)this.outdatedCount / (double)this.totalDependencies;
   }

   public static DependencyHealth empty() {
      return new DependencyHealth(List.of(), 0, 0, 0, 0, false);
   }
}
