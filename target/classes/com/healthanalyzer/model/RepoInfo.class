// Source code is decompiled from a .class file using FernFlower decompiler (from Intellij IDEA).
package com.healthanalyzer.model;

import java.time.Instant;
import java.util.List;

public record RepoInfo(String fullName, String description, String language, int stars, int forks, int watchers, int openIssues, int closedIssues, Instant createdAt, Instant updatedAt, Instant pushedAt, boolean archived, boolean disabled, boolean fork, String license, List<String> topics, String defaultBranch, String homePage) {
   public int totalIssues() {
      return this.openIssues + this.closedIssues;
   }

   public boolean hasIssues() {
      return this.openIssues > 0 || this.closedIssues > 0;
   }

   public String owner() {
      int idx = this.fullName.indexOf(47);
      return idx > 0 ? this.fullName.substring(0, idx) : this.fullName;
   }

   public String name() {
      int idx = this.fullName.indexOf(47);
      return idx > 0 ? this.fullName.substring(idx + 1) : this.fullName;
   }
}
