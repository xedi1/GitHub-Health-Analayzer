// Source code is decompiled from a .class file using FernFlower decompiler (from Intellij IDEA).
package com.healthanalyzer.model;

public record DocumentationInfo(boolean hasReadme, String readmeFilename, int readmeSize, boolean hasContributing, boolean hasCodeOfConduct, boolean hasIssueTemplate, boolean hasPRTemplate, boolean hasChangelog, boolean hasLicense, String licenseType, boolean hasDocsFolder, boolean hasWiki, boolean hasPages) {
   public int score() {
      int score = 0;
      if (this.hasReadme) {
         score += 2;
      }

      if (this.hasContributing) {
         score += 2;
      }

      if (this.hasCodeOfConduct) {
         ++score;
      }

      if (this.hasIssueTemplate) {
         ++score;
      }

      if (this.hasPRTemplate) {
         ++score;
      }

      if (this.hasChangelog) {
         ++score;
      }

      if (this.hasLicense) {
         ++score;
      }

      if (this.hasDocsFolder) {
         ++score;
      }

      return score;
   }

   public boolean isComprehensive() {
      return this.score() >= 6;
   }

   public static DocumentationInfo empty() {
      return new DocumentationInfo(false, (String)null, 0, false, false, false, false, false, false, (String)null, false, false, false);
   }
}
